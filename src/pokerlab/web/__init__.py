"""Local drill web UI (M2; plan §3 "UI pinned for M2"; impl doc §3 Slice E).

FastAPI app + a no-build static drill loop. `pokerlab.web.app:create_app`
builds the app; `main` is the `pokerlab-web` console-script runner.
"""
